/**
 * The persisted domain model. Import from '../types' (this barrel).
 * UI-only shapes (view models, sheet targets) live beside the code that owns them.
 */
export * from './meal'
export * from './plan'
export * from './settings'
